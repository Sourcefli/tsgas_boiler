{
  "name": "asb_clasp_source_tracking",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "build": "node rebuildRootDir.js && tsc --pretty",
    "deploy": "node rebuildRootDir.js && tsc --pretty && clasp push",
    "watch": "node rebuildRootDir.js && tsc --pretty && clasp push --watch"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "@types/google-apps-script": "^1.0.11",
    "gas-lib": "^2.0.3"
  },
  "devDependencies": {
    "fs-extra": "^9.0.0",
    "mkdirp": "^1.0.3"
  }
}